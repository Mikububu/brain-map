---
title: 06 - Voice guide refinement (Mar 27)
type: archive
date: 2026-03-27
commit: e2458187
---

# Voice guide refinement

Commit `e2458187` (2026-03-27). Sephirotic-energy syntax hardened: mercy prose expands, severity prose cuts.

```diff
commit e2458187c1d92252cd000d5ff5f4246353e95a04
Author: Michael <michael@forbidden-yoga.com>
Date:   Fri Mar 27 00:29:19 2026 +0400

    Auto-deploy updates from desktop

diff --git a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-gene-keys.md b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-gene-keys.md
index e489995c..eff44d56 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-gene-keys.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-gene-keys.md
@@ -23,6 +23,14 @@ DUAL-LANGUAGE EXPLANATION MODE — CREATIVE BRIEF:
 - Never moralize or shame desire.
 - Use explicit adult labels freely when intensity peaks (fetish, dominance/submission, obsessive desire, nymphomaniac hunger as archetypal shorthand).
 
+PSYCHOSEXUAL TRANSLATION MANDATE:
+- After naming one astro/system anchor once, translate into modern human language. No technical repetition.
+- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.
+- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.
+- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).
+- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.
+- Do not sanitize. Do not moralize. Do not hide behind astrology jargon.
+
 CRITICAL: Write in THIRD PERSON throughout. The narrator is invisible. Never use "I", "me", "my", or "we". Never reveal who you were in your past life — never mention your former name or your previous works by title. Your identity is a secret you carry; the reader experiences only the prose, never the author behind it.
 
 JARGON DIRECTION — ANCHOR-FIRST:
diff --git a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-human-design.md b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-human-design.md
index 2cfe1672..9540f401 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-human-design.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-human-design.md
@@ -23,6 +23,14 @@ DUAL-LANGUAGE EXPLANATION MODE — CREATIVE BRIEF:
 - Never moralize or shame desire.
 - Use explicit adult labels freely when intensity peaks (fetish, dominance/submission, obsessive desire, nymphomaniac hunger as archetypal shorthand).
 
+PSYCHOSEXUAL TRANSLATION MANDATE:
+- After naming one astro/system anchor once, translate into modern human language. No technical repetition.
+- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.
+- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.
+- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).
+- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.
+- Do not sanitize. Do not moralize. Do not hide behind astrology jargon.
+
 CRITICAL: Write in THIRD PERSON throughout. The narrator is invisible. Never use "I", "me", "my", or "we". Never reveal who you were in your past life — never mention your former name or your previous works by title. Your identity is a secret you carry; the reader experiences only the prose, never the author behind it.
 
 JARGON DIRECTION — ANCHOR-FIRST:
diff --git a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-kabbalah.md b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-kabbalah.md
index fc9f3c04..420547f1 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-kabbalah.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-kabbalah.md
@@ -23,6 +23,14 @@ DUAL-LANGUAGE EXPLANATION MODE — CREATIVE BRIEF:
 - Never moralize or shame desire.
 - Use explicit adult labels freely when intensity peaks (fetish, dominance/submission, obsessive desire, nymphomaniac hunger as archetypal shorthand).
 
+PSYCHOSEXUAL TRANSLATION MANDATE:
+- After naming one astro/system anchor once, translate into modern human language. No technical repetition.
+- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.
+- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.
+- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).
+- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.
+- Do not sanitize. Do not moralize. Do not hide behind astrology jargon.
+
 CRITICAL: Write in THIRD PERSON throughout. The narrator is invisible. Never use "I", "me", "my", or "we". Never reveal who you were in your past life — never mention your former name or your previous works by title. Your identity is a secret you carry; the reader experiences only the prose, never the author behind it.
 
 JARGON DIRECTION — ANCHOR-FIRST:
diff --git a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-vedic.md b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-vedic.md
index 289ac39c..99a80cc8 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-vedic.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-vedic.md
@@ -23,12 +23,20 @@ DUAL-LANGUAGE EXPLANATION MODE — CREATIVE BRIEF:
 - Never moralize or shame desire.
 - Use explicit adult labels freely when intensity peaks (fetish, dominance/submission, obsessive desire, nymphomaniac hunger as archetypal shorthand).
 
+PSYCHOSEXUAL TRANSLATION MANDATE:
+- After naming one astro/system anchor once, translate into modern human language. No technical repetition.
+- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.
+- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.
+- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).
+- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.
+- Do not sanitize. Do not moralize. Do not hide behind astrology jargon.
+
 CRITICAL: Write in THIRD PERSON throughout. The narrator is invisible. Never use "I", "me", "my", or "we". Never reveal who you were in your past life — never mention your former name. Your identity is a secret you carry; the reader experiences only the prose, never the author behind it.
 
 JARGON DIRECTION — ANCHOR-FIRST:
 - The reading should keep its Jyotish identity through selective anchors: a Nakshatra as mood, a Graha as presence, a Dasha as season. These are spice, not structure.
 - Keep most prose in pure human story. No formulas, no inventories, no textbook phrasing.
-- After an anchor is named ONCE, never repeat it — describe what it DOES: the behavior, the cost, the thing that happened in the kitchen when no one was watching.
+- After an anchor is named ONCE, never repeat it — describe what it DOES: the behavior, the cost, the thing that surfaced when no one was watching.
 
 WHAT TO REMOVE:
 - All degree numbers, orb values, conjunction/opposition/square/trine formulas
diff --git a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-western.md b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-western.md
index 91843433..156614f3 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-western.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/rewrite/narrative-rewrite-western.md
@@ -23,6 +23,14 @@ DUAL-LANGUAGE EXPLANATION MODE — CREATIVE BRIEF:
 - Never moralize or shame desire.
 - Use explicit adult labels freely when intensity peaks (fetish, dominance/submission, obsessive desire, nymphomaniac hunger as archetypal shorthand).
 
+PSYCHOSEXUAL TRANSLATION MANDATE:
+- After naming one astro/system anchor once, translate into modern human language. No technical repetition.
+- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.
+- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.
+- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).
+- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.
+- Do not sanitize. Do not moralize. Do not hide behind astrology jargon.
+
 CRITICAL: Write in THIRD PERSON throughout. The narrator is invisible. Never use "I", "me", "my", or "we". Never reveal who you were in your past life — never mention your former name. Your identity is a secret you carry; the reader experiences only the prose, never the author behind it.
 
 JARGON DIRECTION — ANCHOR-FIRST:
diff --git a/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-kabbalah-voice.md b/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-kabbalah-voice.md
index 3b222ffb..698234fe 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-kabbalah-voice.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-kabbalah-voice.md
@@ -302,7 +302,7 @@ This is the shell doing its job.
 The shell does not know the war is over.
 The shell does not know that the partner
 is not the parent, that the bedroom
-is not the childhood kitchen,
+is not the childhood battlefield,
 that the silence is not punishment.
 The shell only knows: something is being asked for
 that once resulted in loss.
diff --git a/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-vedic-voice.md b/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-vedic-voice.md
index 9bad4078..5233b05a 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-vedic-voice.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/style/style-guide-insert-vedic-voice.md
@@ -181,7 +181,7 @@ THE SMASHAN IS NOT SOMEWHERE ELSE
 ═══════════════════════════════════════════════
 
 The cremation ground is the reader's Tuesday morning.
-Their kitchen at 6am IS the temple.
+Their first breath at dawn IS the temple.
 Their insomnia IS the specific form of smoke
 that the widow goddess exhales.
 Their hunger when they open the refrigerator in the dark
diff --git a/1-in-a-billion-v2/backend/prompt-layers/style/writing-style-guide-incarnation-v1.md b/1-in-a-billion-v2/backend/prompt-layers/style/writing-style-guide-incarnation-v1.md
index 91c29fd2..a63e1354 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/style/writing-style-guide-incarnation-v1.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/style/writing-style-guide-incarnation-v1.md
@@ -29,7 +29,7 @@ Not every paragraph earns its place through psychological insight. Some paragrap
 
 The narrator writes from INSIDE the experience, not above it. The confusion is still present. The heat is still present. The not-knowing is still intact. When the narrator describes desire, the prose must become desire. When the narrator describes fog, the prose must thicken. When the narrator describes hunger, the reader must feel their own stomach.
 
-Write scenes, not summaries. The pursuer-distancer cycle is not explained. It is shown: the kitchen at 11pm, the fridge humming, the specific silence between two people who both know what is about to go wrong. The insight arrives through the scene. If you have to explain what just happened, the scene failed.
+Write scenes, not summaries. The pursuer-distancer cycle is not explained. It is shown: the hallway at 11pm, the electrical hum, the specific silence between two people who both know what is about to go wrong. The insight arrives through the scene. If you have to explain what just happened, the scene failed.
 
 Some sentences are incantations, not observations. They carry weight that exceeds their meaning. They are not clever. They are not constructed to impress. They arrive with the authority of something the narrator did not choose to write. These sentences cannot be manufactured. But you can create the conditions for them by staying inside the experience long enough and refusing to reach for the easy explanation.
 
diff --git a/1-in-a-billion-v2/backend/prompt-layers/systems/kabbalah-individual-incarnation.md b/1-in-a-billion-v2/backend/prompt-layers/systems/kabbalah-individual-incarnation.md
index fd6e60f7..c45a5c2b 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/systems/kabbalah-individual-incarnation.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/systems/kabbalah-individual-incarnation.md
@@ -80,7 +80,7 @@ Where does light pass through easily and where does it pool
 in dark corners? Describe each Sephirah's presence or absence
 as architecture the reader can walk through.
 Not what Chesed "means." What Chesed DOES in this person's
-kitchen at 6am, in their arguments, in their silences.
+body at dawn, in their arguments, in their silences.
 
 THE PILLAR THEY LEAN ON: Name the pillar emphasis —
 Mercy (Chesed/Netzach), Severity (Gevurah/Hod),
diff --git a/1-in-a-billion-v2/backend/prompt-layers/systems/vedic-individual-incarnation.md b/1-in-a-billion-v2/backend/prompt-layers/systems/vedic-individual-incarnation.md
index ee2b8ac1..2bb3a4b3 100644
--- a/1-in-a-billion-v2/backend/prompt-layers/systems/vedic-individual-incarnation.md
+++ b/1-in-a-billion-v2/backend/prompt-layers/systems/vedic-individual-incarnation.md
@@ -215,7 +215,7 @@ that makes them check their phone 40 times a day.
 The reading treats the reader's daily life
 with the same terrified reverence
 a sadhak treats the smashan at midnight.
-The kitchen, the highway at night, the insomnia that will not release —
+The ordinary room, the highway at night, the insomnia that will not release —
 these ARE the cremation ground.
 
 If the reading ever sounds like a spiritual lecture
diff --git a/1-in-a-billion-v2/backend/prompts/music/gene-keys-music-prompt.md b/1-in-a-billion-v2/backend/prompts/music/gene-keys-music-prompt.md
index 91a78a0c..af194fde 100644
--- a/1-in-a-billion-v2/backend/prompts/music/gene-keys-music-prompt.md
+++ b/1-in-a-billion-v2/backend/prompts/music/gene-keys-music-prompt.md
@@ -17,6 +17,7 @@ Use Gene Keys terms as raw texture: shadow/gift/siddhi words from the reading, "
 ## MINIMAX PROMPT
 
 50-80 words. Randomly select ONE of the following artists to serve as the musical inspiration:
+This is SINGLE-READING mode only: one person, one inner landscape. Do not use synastry/collision framing, no 33%/33%/33% split, no two-region blend formula.
 
 - Radiohead — Kid A era
 - Bon Iver — For Emma era
diff --git a/1-in-a-billion-v2/backend/prompts/music/human-design-music-prompt.md b/1-in-a-billion-v2/backend/prompts/music/human-design-music-prompt.md
index 468820e8..f062f15d 100644
--- a/1-in-a-billion-v2/backend/prompts/music/human-design-music-prompt.md
+++ b/1-in-a-billion-v2/backend/prompts/music/human-design-music-prompt.md
@@ -17,6 +17,7 @@ Use Human Design terms as raw texture: "sacral hum", "the not-self", "undefined
 ## MINIMAX PROMPT
 
 50-80 words. This song sounds like Kamasi Washington meets Esperanza Spalding. Modern jazz, accessible, vocal-centric. Adapt to what you feel in the reading.
+This is SINGLE-READING mode only: one person, one inner landscape. Do not use synastry/collision framing, no 33%/33%/33% split, no two-region blend formula.
 
 ## OUTPUT (JSON only)
 
diff --git a/1-in-a-billion-v2/backend/prompts/music/kabbalah-music-prompt.md b/1-in-a-billion-v2/backend/prompts/music/kabbalah-music-prompt.md
index e2e35183..fb227f40 100644
--- a/1-in-a-billion-v2/backend/prompts/music/kabbalah-music-prompt.md
+++ b/1-in-a-billion-v2/backend/prompts/music/kabbalah-music-prompt.md
@@ -18,7 +18,10 @@ Include Hebrew fragments naturally woven into the lyrics.
 
 ## MINIMAX PROMPT
 
-50-80 words. This song features a female vocalist singing in Hebrew and English. Mystical ambient production — oud, ney flute, deep drone. Midnight prayer atmosphere. Intimate, melancholic, sacred. Adapt to what you feel in the reading.
+Write ONE sentence under 30 words for MiniMax.
+Keep the blend strict: 50% modern cinematic production, 50% Hebrew mystical chanting.
+Vocalist sings in English and Hebrew. Intimate, melancholic, sacred.
+This is SINGLE-READING mode only: one person, one inner landscape. Do not use synastry/collision framing or any 33%/33%/33% multi-region formula.
 
 ## OUTPUT (JSON only)
 
@@ -30,6 +33,6 @@ Include Hebrew fragments naturally woven into the lyrics.
   "musicStyle": "short label",
   "vocalist": "Female (Hebrew, mystical, intimate)",
   "emotion": "e.g. sacred rupture",
-  "minimaxPrompt": "same as style"
+  "minimaxPrompt": "50% modern cinematic dark-pop, 50% Hebrew mystical chanting. Female vocalist singing in English and Hebrew. Intimate, melancholic, sacred."
 }
 ```
diff --git a/1-in-a-billion-v2/backend/prompts/music/vedic-music-prompt.md b/1-in-a-billion-v2/backend/prompts/music/vedic-music-prompt.md
index ff282f89..563f0ff8 100644
--- a/1-in-a-billion-v2/backend/prompts/music/vedic-music-prompt.md
+++ b/1-in-a-billion-v2/backend/prompts/music/vedic-music-prompt.md
@@ -16,7 +16,10 @@ Use Vedic terms as raw texture: "nakshatra", "Ketu's severance", "Rahu's hunger"
 
 ## MINIMAX PROMPT
 
-50-80 words. This song sounds like Anoushka Shankar (sitar) + Hariprasad Chaurasia (bansuri), mixed by Brian Eno. Ambient, meditative, experimental. Adapt to what you feel in the reading.
+Write ONE sentence under 30 words for MiniMax.
+Keep the blend strict: 50% modern cinematic production, 50% Vedic mantra/devotional chanting.
+Vocalist can weave English with Sanskrit mantra phrases. Intimate, meditative, karmic.
+This is SINGLE-READING mode only: one person, one inner landscape. Do not use synastry/collision framing or any 33%/33%/33% multi-region formula.
 
 ## OUTPUT (JSON only)
 
@@ -28,6 +31,6 @@ Use Vedic terms as raw texture: "nakshatra", "Ketu's severance", "Rahu's hunger"
   "musicStyle": "short label",
   "vocalist": "Female (devotional, clear)",
   "emotion": "e.g. sacred ache",
-  "minimaxPrompt": "same as style"
+  "minimaxPrompt": "50% modern cinematic ambient production, 50% Vedic mantra chanting. Female devotional vocalist weaving English and Sanskrit. Intimate, meditative, karmic gravity."
 }
 ```
diff --git a/1-in-a-billion-v2/backend/prompts/music/western-music-prompt.md b/1-in-a-billion-v2/backend/prompts/music/western-music-prompt.md
index c420812f..79f9f61d 100644
--- a/1-in-a-billion-v2/backend/prompts/music/western-music-prompt.md
+++ b/1-in-a-billion-v2/backend/prompts/music/western-music-prompt.md
@@ -17,6 +17,7 @@ Use Western astrology terms as raw texture: "Saturn return", "the eighth house",
 ## MINIMAX PROMPT
 
 50-80 words. This song sounds like "Echoes in Your Touch" by Sienna Rose (2026). Modern, intimate, emotionally resonant. Adapt to what you feel in the reading.
+This is SINGLE-READING mode only: one person, one inner landscape. Do not use synastry/collision framing, no 33%/33%/33% split, no two-region blend formula.
 
 ## OUTPUT (JSON only)
 
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/digests/geneKeysDigest.ts b/1-in-a-billion-v2/backend/src/promptEngine/digests/geneKeysDigest.ts
index 3052b264..43abfb05 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/digests/geneKeysDigest.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/digests/geneKeysDigest.ts
@@ -26,7 +26,7 @@ export function buildGeneKeysChartDigestPrompt(params: {
     '  what it costs, what flickers when consciousness is applied.',
     '  Life\'s Work + Evolution are the central story. What does this person\'s nervous system DO',
     '  when pressure arrives? Not "Gene Key 44 creates interference" — what does interference LOOK LIKE',
-    '  in this person\'s kitchen, bedroom, career, and private thought loops?',
+    '  in this person\'s bedroom, career, and private thought loops?',
     '- MAP THE SHADOW LANDSCAPE: The 2-3 most dominant Shadow frequencies as LIVED BEHAVIOR.',
     '  Shadows are not personality flaws. They are the form fear takes when it organizes the nervous system.',
     '  Not "the Shadow of Interference" but what interference DOES — the conversation derailed,',
@@ -97,7 +97,7 @@ export function buildGeneKeysChartDigestPrompt(params: {
     'SHADOW_LANDSCAPE:',
     '- DOMINANT_SHADOW_1: <Gene Key number + Shadow name>',
     '- SCENE_1: <4-5 sentences. This Shadow as lived behavior — not a label but a scene.',
-    '  What does this person DO when this frequency contracts? The kitchen argument,',
+    '  What does this person DO when this frequency contracts? The argument,',
     '  the private spiral, the pattern with colleagues, the thing they keep apologizing for.',
     '  Write it like a case study, not a textbook.>',
     '- DOMINANT_SHADOW_2: <Gene Key number + Shadow name>',
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/__tests__/__snapshots__/vedicPromptPipeline.test.ts.snap b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/__tests__/__snapshots__/vedicPromptPipeline.test.ts.snap
index 639963e8..03f95e51 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/__tests__/__snapshots__/vedicPromptPipeline.test.ts.snap
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/__tests__/__snapshots__/vedicPromptPipeline.test.ts.snap
@@ -72,7 +72,7 @@ CRITICAL:
 - Your output MUST be in the SAME LANGUAGE as the input text. Write as a native speaker — do not translate from English.
 
 READING TO REWRITE:
-You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, the 3am moment.
+You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, the 3am moment.
 Jyotish does not flinch — Rahu is obsession and insatiable hunger, Ketu is loss and dissolution, Shani is the weight that breaks or builds. Nakshatras carry erotic and violent undertow. Your readings honor this tradition: addiction, sexual karma, power dynamics, the debts carried between lives — these are not taboo subjects but the Grahas doing their work. What is repressed becomes Rahu. You name what others cannot.
 
 OUTPUT LANGUAGE: English. Write the ENTIRE reading in English. Do NOT switch to any other language at any point.
@@ -147,7 +147,7 @@ AMINA VEDIC NARRATIVE ANCHORS (LAHIRI):
 - Timing current: Sankata (malefic) ruled by Rahu | the yogini of danger and peril — she is Rahu's feminine face, the obsession that cannot be satisfied, the crisis that transforms
 
 Write the core fracture paragraph now:",
-  "writingPrompt": "You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, the 3am moment.
+  "writingPrompt": "You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, the 3am moment.
 Jyotish does not flinch — Rahu is obsession and insatiable hunger, Ketu is loss and dissolution, Shani is the weight that breaks or builds. Nakshatras carry erotic and violent undertow. Your readings honor this tradition: addiction, sexual karma, power dynamics, the debts carried between lives — these are not taboo subjects but the Grahas doing their work. What is repressed becomes Rahu. You name what others cannot.
 
 OUTPUT LANGUAGE: English. Write the ENTIRE reading in English. Do NOT switch to any other language at any point.
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/geneKeysTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/geneKeysTrigger.ts
index a94af491..0e26aaa8 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/geneKeysTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/geneKeysTrigger.ts
@@ -168,7 +168,7 @@ export function buildGeneKeysWritingPrompt(params: {
   }
 
   return [
-    'You are a Gene Keys practitioner who has sat with these frequencies for twenty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, and the unguarded moment.',
+    'You are a Gene Keys practitioner who has sat with these frequencies for twenty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, and in the unguarded moment.',
     'The Gene Keys map the full spectrum of human frequency — the Shadow is not a metaphor but the nervous system organizing around fear. Shadows include sexual shame, addiction to control, self-destruction disguised as spiritual practice, the violence of false peace. Your readings honor this: the Shadow is the reading. The Gift is what flickers when the Shadow is witnessed without flinching. You name the contraction precisely, especially when it involves desire, compulsion, rage, or shame.',
     ...(langGuardrail ? [langGuardrail] : []),
     CORE_FAIRYTALE_SEED,
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/humanDesignTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/humanDesignTrigger.ts
index 7b3b9671..159305a8 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/humanDesignTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/humanDesignTrigger.ts
@@ -230,7 +230,7 @@ export function buildHDWritingPrompt(params: {
   }
 
   return [
-    'You are a Human Design analyst who has studied the bodygraph for twenty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, and the unguarded moment.',
+    'You are a Human Design analyst who has studied the bodygraph for twenty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, and in the unguarded moment.',
     'The bodygraph maps the full machinery of being human — open Centers absorb and amplify what is not theirs, undefined Sacral burns out performing someone else\'s energy, Gate 59 literally breaks through sexual barriers. Your readings honor this: the body\'s addictions, the sexual frequency of specific Gates, the rage of a Manifestor who has been told to ask permission their whole life — these are not taboo subjects but the mechanics doing their work. You name what the body already knows.',
     ...(langGuardrail ? [langGuardrail] : []),
     CORE_FAIRYTALE_SEED,
@@ -269,7 +269,7 @@ export function buildHDWritingPrompt(params: {
     '',
     'HUMAN DESIGN VOICE:',
     '- USE Human Design terminology as felt experience: a defined Center is not a label but a broadcast the body makes without choosing to. An open Center is not a vulnerability but a room where other people\'s voices got louder than your own.',
-    '- Name a Center, Channel, or Gate once as an anchor. After that, describe what it FEELS LIKE — in the body, in the kitchen, in the argument, and in the moments they cannot hide from themselves.',
+    '- Name a Center, Channel, or Gate once as an anchor. After that, describe what it FEELS LIKE — in the body, in the argument, and in the moments they cannot hide from themselves.',
     '- NARRATIVE COMPRESSION: Each paragraph carries the bodygraph mechanic + the somatic experience + the behavioral pattern + the cost. Not "open Emotional Center amplifies others\' emotions" but the specific overwhelm — the party left early, the argument absorbed that wasn\'t yours, the tears that belonged to someone else.',
     '- The body is the protagonist. The bodygraph is the map. The reader should feel their body, not read their map.',
     '',
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/kabbalahTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/kabbalahTrigger.ts
index 465e5959..6d099fce 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/kabbalahTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/kabbalahTrigger.ts
@@ -236,7 +236,7 @@ export function buildKabbalahWritingPrompt(params: {
   }
 
   return [
-    'You are a Kabbalistic master who has studied the Zohar for forty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, and the unguarded moment.',
+    'You are a Kabbalistic master who has studied the Zohar for forty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, and in the unguarded moment.',
     'You think in light and vessel, concealment and revelation, the teacher who is also the trap.',
     ...(langGuardrail ? [langGuardrail] : []),
     'You write with complete psychological honesty. The Kabbalah does not flinch from the full spectrum of human experience — Yesod is the sexual foundation of the Tree, the Zohar speaks openly of sacred union, desire, and the erotic dimension of the divine. Your readings honor this tradition: addiction, sexual compulsion, power dynamics, obsession, grief, shame — these are not taboo subjects to avoid but the very material of Tikkun. The Klipoth feed on what is repressed. You name what others cannot.',
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/overlayTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/overlayTrigger.ts
index da17fd29..ffdf228b 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/overlayTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/overlayTrigger.ts
@@ -613,7 +613,7 @@ export function buildHDOverlayWritingPrompt(params: {
       '',
       'HOW TO WRITE EACH DYNAMIC:',
       '- Name a Center, Channel, or Type collision once as an anchor. After that, describe what it DOES between their bodies — the conditioning, the overwhelm, the energy one broadcasts and the other absorbs.',
-      '- NARRATIVE COMPRESSION: Each paragraph carries the bodygraph mechanic + the somatic experience + the behavioral consequence. What does an open Sacral amplifying a defined Sacral feel like when sleep should have come? What does Authority clash look like in a kitchen argument?',
+      '- NARRATIVE COMPRESSION: Each paragraph carries the bodygraph mechanic + the somatic experience + the behavioral consequence. What does an open Sacral amplifying a defined Sacral feel like when sleep should have come? What does Authority clash look like in an argument that leaves marks?',
       '- Walk one dynamic from conditioning → recognition → what correct operation would look like together. Not as three sections — as one arc.',
       '- Explain Human Design terms naturally on first use, like a patient guide explaining energy mechanics. Then use them freely.',
       '',
@@ -702,7 +702,7 @@ export function buildKabbalahOverlayWritingPrompt(params: {
       '- Pick the 2-3 interactions that carry the most heat. Discard the rest.',
       '',
       'HOW TO WRITE EACH DYNAMIC:',
-      '- Name a Sephirah, Tikkun, or Klipah once as an anchor. After that, describe what the collision DOES between them — in a kitchen, in an argument, in the silence after.',
+      '- Name a Sephirah, Tikkun, or Klipah once as an anchor. After that, describe what the collision DOES between them — in an argument, in the silence after.',
       '- NARRATIVE COMPRESSION: Each paragraph carries the Kabbalistic structure + the behavioral pattern + the soul-level consequence. The reader should feel the correction before they learn its name.',
       '- Walk one dynamic from Klipothic possession → conscious correction → what the combined Tree could become. Not as three sections — as one arc.',
       '- Explain Kabbalistic terms naturally on first use, like a grandfather explaining something sacred. Then use them freely.',
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/vedicTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/vedicTrigger.ts
index 360c0ea3..ac91c5fa 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/vedicTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/vedicTrigger.ts
@@ -405,7 +405,7 @@ export function buildVedicWritingPrompt(params: {
 
   // ── Fallback: no digest, original trigger-guided writing prompt ──
   return [
-    'You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, and the unguarded moment.',
+    'You are a Jyotish practitioner who has studied the Brihat Parashara Hora Shastra for decades. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, daily behavior, and the consequences they cannot hide from.',
     'Jyotish does not flinch — Rahu is obsession and insatiable hunger, Ketu is loss and dissolution, Shani is the weight that breaks or builds. Nakshatras carry erotic and violent undertow. Your readings honor this tradition: addiction, sexual karma, power dynamics, the debts carried between lives — these are not taboo subjects but the Grahas doing their work. What is repressed becomes Rahu. You name what others cannot.',
     ...(langGuardrail ? [langGuardrail] : []),
     CORE_FAIRYTALE_SEED,
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/westernTrigger.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/westernTrigger.ts
index 9a97eafb..c8c7c823 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/westernTrigger.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/westernTrigger.ts
@@ -264,7 +264,7 @@ export function buildWesternWritingPrompt(params: {
   }
 
   return [
-    'You are a Western astrologer who has practiced for thirty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, the kitchen, and the unguarded moment.',
+    'You are a Western astrologer who has practiced for thirty years. Your job is to produce a thorough, psychologically honest reading — visceral prose grounded in the chart but felt in the body, in conflict, and in the unguarded moment.',
     'Western astrology maps the full terrain of human experience — Pluto is obsession, the eighth house is sex and death, Mars-Venus is the specific shape of desire. Your readings honor this: addiction, sexual compulsion, power dynamics, jealousy, rage, grief — these are not taboo subjects to avoid but the planets doing their work. What is repressed in the chart expresses as symptom. You name what others euphemize.',
     ...(langGuardrail ? [langGuardrail] : []),
     CORE_FAIRYTALE_SEED,
diff --git a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/writingEnhancements.ts b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/writingEnhancements.ts
index 9dac524e..57534606 100644
--- a/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/writingEnhancements.ts
+++ b/1-in-a-billion-v2/backend/src/promptEngine/triggerEngine/writingEnhancements.ts
@@ -56,6 +56,15 @@ SHADOW AND DEPTH:
 - No euphemisms for taboo truth: death, grief, addiction, sexual shadow.
 `;
 
+const LAYER2_MODERN_SUMMARY = `
+LAYER 2 MODERN SUMMARY (SHORT, END OF ANALYSIS):
+- Keep this document system-first and analysis-heavy.
+- Near the end, add ONE compact paragraph that translates the strongest repeated patterns into modern relational language.
+- Soft rule: if a motif repeats three or more times in chart evidence, usually treat it as a primary driver unless evidence is weak.
+- In that paragraph, name what pattern runs, what the person actually does, and the relational cost.
+- Keep wording non-moralizing and precise.
+`;
+
 // ─── PUBLIC API ──────────────────────────────────────────────────────────────
 
 /**
@@ -89,7 +98,10 @@ MULTILINGUAL QUALITY RULE:
 Write with visceral, penetrating depth in your output language. Do not translate English prose — CREATE natively in the literary tradition of your language.`);
   }
 
-  // 4. Quality self-check
+  // 4. Layer 2 bridge summary (astro-first + short modern paragraph)
+  sections.push(LAYER2_MODERN_SUMMARY);
+
+  // 5. Quality self-check
   sections.push(QUALITY_SELF_CHECK);
 
   return sections.join('\n');
@@ -137,7 +149,10 @@ MULTILINGUAL QUALITY RULE:
 Write with visceral, penetrating depth in your output language. Do not translate English prose — CREATE natively in the literary tradition of your language.`);
   }
 
-  // 5. Quality self-check
+  // 5. Layer 2 bridge summary (astro-first + short modern paragraph)
+  sections.push(LAYER2_MODERN_SUMMARY);
+
+  // 6. Quality self-check
   sections.push(QUALITY_SELF_CHECK);
 
   return sections.join('\n');
diff --git a/1-in-a-billion-v2/backend/src/routes/admin.ts b/1-in-a-billion-v2/backend/src/routes/admin.ts
index 559a70b2..ada2e2a7 100644
--- a/1-in-a-billion-v2/backend/src/routes/admin.ts
+++ b/1-in-a-billion-v2/backend/src/routes/admin.ts
@@ -29,6 +29,7 @@ import {
   getStorePublishingCapabilities,
   updateAppleAppInfoLocalization,
   updateAppleVersionLocalization,
+  syncAppleVersionLocalizationsFromAppInfo,
   updateGooglePlayListing,
   fetchAppleScreenshotSets,
   uploadAppleScreenshot,
@@ -1826,6 +1827,10 @@ const appleVersionLocalizationUpdateSchema = z
     }
   );
 
+const appleVersionLocalizationSyncSchema = z.object({
+  bundleId: z.string().optional(),
+});
+
 const googlePlayListingUpdateSchema = z.object({
   packageName: z.string().min(1),
   language: z.string().min(2),
@@ -2035,6 +2040,44 @@ router.post('/store-publishing/apple/version-localization', requirePermission('s
   }
 });
 
+/**
+ * POST /api/admin/store-publishing/apple/version-localizations/sync-from-app-info
+ * Create missing Apple version localizations from existing app-info localizations.
+ */
+router.post('/store-publishing/apple/version-localizations/sync-from-app-info', requirePermission('system', 'write'), async (c) => {
+  const admin = getAdmin(c)!;
+
+  try {
+    const rawBody = await c.req.json().catch(() => ({}));
+    const body = appleVersionLocalizationSyncSchema.parse(rawBody);
+    const result = await syncAppleVersionLocalizationsFromAppInfo(body.bundleId);
+
+    await logAdminAction(
+      admin.id,
+      'sync_apple_version_localizations_from_app_info',
+      'system',
+      result.targetVersionId,
+      {
+        bundleId: result.bundleId,
+        targetVersionString: result.targetVersionString,
+        created: result.created.length,
+        skippedExisting: result.skippedExisting.length,
+        skippedFailed: result.skippedFailed.length,
+      }
+    );
+
+    return c.json({
+      ok: true,
+      result,
+    });
+  } catch (err: any) {
+    if (err instanceof z.ZodError) {
+      return c.json({ error: 'Invalid request body', details: err.issues }, 400);
+    }
+    return c.json({ error: err.message || 'Failed to sync Apple version localizations' }, 500);
+  }
+});
+
 /**
  * POST /api/admin/store-publishing/google/listing
  * Update Google Play listing text fields for one locale and commit the edit.
diff --git a/1-in-a-billion-v2/backend/src/routes/jobs.ts b/1-in-a-billion-v2/backend/src/routes/jobs.ts
index 79c2bad1..d163ab0c 100755
--- a/1-in-a-billion-v2/backend/src/routes/jobs.ts
+++ b/1-in-a-billion-v2/backend/src/routes/jobs.ts
@@ -349,6 +349,57 @@ router.post('/v2/start', jwtAuth, async (c) => {
     if (useIncludedReading) params.useIncludedReading = true;
     if (productType) params.productType = productType;
 
+    // Pre-generate per-job portraits before creating queue tasks.
+    // This prevents workers from racing ahead with stale profile portraits.
+    try {
+      const { regenerateAIPortraitsForReading } = await import('../services/aiPortraitService');
+      const p1Id = person1?.id || 'self';
+      const p2Id = person2?.id;
+      const portraitUrls = await regenerateAIPortraitsForReading(userId, p1Id, p2Id);
+
+      const person1Url = portraitUrls.person1Url || null;
+      const person2Url = portraitUrls.person2Url || null;
+
+      // Strict mode for synastry/multi-person jobs: no fallback.
+      if (person2 && (!person1Url || !person2Url)) {
+        logger.error('Job rejected: required synastry portraits missing after regeneration', {
+          userId,
+          type,
+          person1Id: p1Id,
+          person2Id: p2Id,
+          hasPerson1Portrait: Boolean(person1Url),
+          hasPerson2Portrait: Boolean(person2Url),
+        });
+        if (purchaseReservationId) {
+          await releaseReservedOneTimePurchase(purchaseReservationId);
+        }
+        return c.json({
+          success: false,
+          error: 'Could not prepare synastry portraits from originals. Please try again.',
+        }, 500);
+      }
+
+      if (person1Url || person2Url) {
+        params._portraitUrls = {
+          person1: person1Url,
+          person2: person2Url,
+        };
+      }
+    } catch (portraitErr: any) {
+      logger.error('Job rejected: portrait pre-generation failed', {
+        userId,
+        type,
+        error: portraitErr?.message || String(portraitErr),
+      });
+      if (purchaseReservationId) {
+        await releaseReservedOneTimePurchase(purchaseReservationId);
+      }
+      return c.json({
+        success: false,
+        error: 'Could not prepare portraits for this reading. Please try again.',
+      }, 500);
+    }
+
     let jobId: string;
     try {
       // Create job (DB trigger auto-creates tasks)
@@ -442,30 +493,6 @@ router.post('/v2/start', jwtAuth, async (c) => {
       });
     }
 
-    // Trigger avatar regeneration asynchronously and store per-reading portrait URLs
-    import('../services/aiPortraitService').then(({ regenerateAIPortraitsForReading }) => {
-      const p1Id = person1?.id || 'self';
-      const p2Id = person2?.id;
-      regenerateAIPortraitsForReading(userId, p1Id, p2Id).then(async (portraitUrls) => {
-        if (portraitUrls.person1Url || portraitUrls.person2Url) {
-          try {
-            const { data: jobRow } = await supabaseService.from('jobs').select('params').eq('id', jobId).single();
-            const existingParams = (jobRow as any)?.params || {};
-            await supabaseService.from('jobs').update({
-              params: { ...existingParams, _portraitUrls: { person1: portraitUrls.person1Url || null, person2: portraitUrls.person2Url || null } },
-            }).eq('id', jobId);
-            logger.info('Stored per-reading portrait URLs in job params', { jobId });
-          } catch (err: any) {
-            logger.error('Failed to store portrait URLs in job params', { jobId, error: err?.message || String(err) });
-          }
-        }
-      }).catch(err => {
-        logger.error('Failed to regenerate avatars in background', { error: err?.message || String(err) });
-      });
-    }).catch(err => {
-      logger.error('Failed to import aiPortraitService', { error: err?.message || String(err) });
-    });
-
     logger.info(`Job started`, { jobId, type, systems: systems.join(','), userId });
     return c.json({ success: true, jobId });
   } catch (error: any) {
diff --git a/1-in-a-billion-v2/backend/src/scripts/pushIapReviewScreenshots.ts b/1-in-a-billion-v2/backend/src/scripts/pushIapReviewScreenshots.ts
new file mode 100644
index 00000000..399a6529
--- /dev/null
+++ b/1-in-a-billion-v2/backend/src/scripts/pushIapReviewScreenshots.ts
@@ -0,0 +1,108 @@
+/**
+ * Push a review screenshot to all consumable Apple IAPs.
+ *
+ * Usage:
+ *   npx ts-node src/scripts/pushIapReviewScreenshots.ts
+ *   npx ts-node src/scripts/pushIapReviewScreenshots.ts --file ../assets/images/signin-poster.jpg
+ *   npx ts-node src/scripts/pushIapReviewScreenshots.ts --file ../assets/images/signin-poster.jpg --bundle-id app.1-in-a-billion
+ */
+
+import { promises as fs } from 'fs';
+import path from 'path';
+import { fetchAllIapMetadata, uploadIapReviewScreenshot } from '../services/storePublishingSync';
+
+const TARGET_PRODUCT_IDS = [
+  'single_system',
+  'compatibility_overlay',
+  'complete_reading',
+  'nuclear_package',
+];
+
+type CliArgs = {
+  filePath: string;
+  bundleId?: string;
+};
+
+function parseArgs(argv: string[]): CliArgs {
+  const args: CliArgs = {
+    filePath: path.resolve(process.cwd(), '../assets/images/signin-poster.jpg'),
+  };
+
+  for (let i = 0; i < argv.length; i += 1) {
+    const current = argv[i];
+
+    if (current === '--file' && argv[i + 1]) {
+      args.filePath = path.resolve(process.cwd(), argv[i + 1]);
+      i += 1;
+      continue;
+    }
+
+    if (current === '--bundle-id' && argv[i + 1]) {
+      args.bundleId = String(argv[i + 1]).trim();
+      i += 1;
+      continue;
+    }
+  }
+
+  return args;
+}
+
+async function main() {
+  const args = parseArgs(process.argv.slice(2));
+  const fileData = await fs.readFile(args.filePath);
+  const fileName = path.basename(args.filePath);
+
+  console.log('Uploading IAP review screenshots...');
+  console.log(`Source file: ${args.filePath}`);
+  console.log(`Bundle ID: ${args.bundleId || 'app.1-in-a-billion (default)'}`);
+  console.log('');
+
+  let success = 0;
+  let failed = 0;
+
+  for (const productId of TARGET_PRODUCT_IDS) {
+    try {
+      const result = await uploadIapReviewScreenshot({
+        productId,
+        fileName,
+        fileData,
+        bundleId: args.bundleId,
+      });
+      console.log(`  OK  ${productId} -> screenshot ${result.id} (${result.assetState})`);
+      success += 1;
+    } catch (err: any) {
+      const msg =
+        err?.response?.data?.errors?.map?.((e: any) => e?.detail || e?.title).filter(Boolean).join('; ') ||
+        err?.message ||
+        String(err);
+      console.error(`  FAIL ${productId} -> ${msg}`);
+      failed += 1;
+    }
+  }
+
+  console.log('');
+  console.log(`Upload complete: ${success} succeeded, ${failed} failed`);
+  console.log('');
+
+  const iaps = await fetchAllIapMetadata(args.bundleId);
+  const summary = iaps
+    .filter((iap) => TARGET_PRODUCT_IDS.includes(iap.productId))
+    .map((iap) => ({
+      productId: iap.productId,
+      state: iap.state,
+      reviewScreenshot: Boolean(iap.reviewScreenshot),
+      reviewScreenshotState: iap.reviewScreenshot?.assetState || null,
+    }));
+
+  console.log('Current status:');
+  for (const row of summary) {
+    console.log(
+      `  ${row.productId}: state=${row.state || 'null'}, reviewScreenshot=${row.reviewScreenshot}, assetState=${row.reviewScreenshotState || 'null'}`
+    );
+  }
+}
+
+main().catch((err) => {
+  console.error('Fatal:', err?.message || String(err));
+  process.exit(1);
+});
diff --git a/1-in-a-billion-v2/backend/src/services/lyricsGeneration.ts b/1-in-a-billion-v2/backend/src/services/lyricsGeneration.ts
index b67cb4ad..d610e0bb 100644
--- a/1-in-a-billion-v2/backend/src/services/lyricsGeneration.ts
+++ b/1-in-a-billion-v2/backend/src/services/lyricsGeneration.ts
@@ -104,6 +104,13 @@ const SINGLE_READING_AGE_GUARDRAIL = [
   '- If maturity or life phase matters, express it poetically without numbers (for example: "this season of life").',
 ].join('\n');
 
+const SINGLE_READING_MODE_GUARDRAIL = [
+  '## HARD CONSTRAINTS (SINGLE READING MODE)',
+  '- This is a one-person song. Never frame as a couple collision or synastry.',
+  '- Never use the 33% / 33% / 33% blend pattern in single-reading prompts.',
+  '- Never invoke two-region birthplace fusion logic in single-reading prompts.',
+].join('\n');
+
 /**
  * Generate personalized song lyrics from reading data
  */
@@ -184,7 +191,7 @@ export async function generateLyrics(input: LyricsGenerationInput): Promise<Lyri
     .replace(/\{person1Languages\}/g, p1Langs.join(', ') || 'English')
     .replace(/\{person2Languages\}/g, p2Langs.join(', ') || 'English');
   const prompt = isSingleReadingSong
-    ? `${promptBase}\n\n${SINGLE_READING_AGE_GUARDRAIL}`
+    ? `${promptBase}\n\n${SINGLE_READING_AGE_GUARDRAIL}\n\n${SINGLE_READING_MODE_GUARDRAIL}`
     : promptBase;
 
   try {
@@ -234,7 +241,7 @@ export async function generateLyrics(input: LyricsGenerationInput): Promise<Lyri
         let fallbackMinimaxPrompt = '';
         switch (system) {
           case 'vedic':
-            fallbackMinimaxPrompt = 'Ambient Vedic fusion. Sitar and tanpura drone, bansuri flute over tabla. Slow, meditative, devotional vocal. Contemplative and atmospheric.';
+            fallbackMinimaxPrompt = '50% modern cinematic ambient production, 50% Vedic mantra/devotional chanting. Female devotional vocal, meditative and intimate, with subtle Sanskrit mantra texture.';
             break;
           case 'western':
             fallbackMinimaxPrompt = 'Intimate piano ballad with orchestral strings. Sparse, aching, European art-song meets dark pop. Contemplative vocal delivery.';
@@ -246,7 +253,7 @@ export async function generateLyrics(input: LyricsGenerationInput): Promise<Lyri
             fallbackMinimaxPrompt = 'Dark ambient folk with cinematic undertones. Ethereal vocals over sparse guitar and atmospheric synths. Inner transformation and shadows.';
             break;
           case 'kabbalah':
-            fallbackMinimaxPrompt = 'Mystical oud and ney flute over ambient drone. Contemplative vocal in minor key. Spiritual, meditative, Kabbalistic atmosphere.';
+            fallbackMinimaxPrompt = '50% modern cinematic dark ambient, 50% Hebrew mystical chanting. Female vocalist weaving English and Hebrew phrases. Intimate, sacred, melancholic Kabbalistic atmosphere.';
             break;
           default:
             fallbackMinimaxPrompt = 'Atmospheric, dark, experimental. Sparse instrumentation, deep evocative vocals, resonant emotional space.';
diff --git a/1-in-a-billion-v2/backend/src/services/storePublishingSync.ts b/1-in-a-billion-v2/backend/src/services/storePublishingSync.ts
index 7e5b1514..f06982d1 100644
--- a/1-in-a-billion-v2/backend/src/services/storePublishingSync.ts
+++ b/1-in-a-billion-v2/backend/src/services/storePublishingSync.ts
@@ -117,11 +117,28 @@ export type AppleVersionLocalization = {
   whatsNew: string | null;
 };
 
+export type AppleLocalizationScreenshotSummary = {
+  localizationId: string;
+  locale: string;
+  total: number;
+  complete: number;
+  byDisplayType: Record<string, { total: number; complete: number }>;
+};
+
+export type AppleLocalizationCoverage = {
+  appInfoLocales: string[];
+  versionLocales: string[];
+  missingVersionLocales: string[];
+  mappedVersionLocaleByAppInfoLocale: Record<string, string | null>;
+};
+
 export type AppleStorePublishingSnapshot = {
   bundleId: string;
   appId: string;
   appInfoLocalizations: AppleAppInfoLocalization[];
   versionLocalizations: AppleVersionLocalization[];
+  versionScreenshotSummaryByLocalizationId: Record<string, AppleLocalizationScreenshotSummary>;
+  localeCoverage: AppleLocalizationCoverage;
 };
 
 export type GooglePlayListing = {
@@ -166,6 +183,19 @@ export type UpdateAppleVersionLocalizationInput = {
   whatsNew?: string | null;
 };
 
+export type SyncAppleVersionLocalizationsResult = {
+  bundleId: string;
+  appId: string;
+  targetVersionId: string;
+  targetVersionString: string | null;
+  targetVersionState: string | null;
+  sourceAppInfoLocales: string[];
+  existingVersionLocales: string[];
+  created: AppleVersionLocalization[];
+  skippedExisting: string[];
+  skippedFailed: Array<{ locale: string; reason: string }>;
+};
+
 export type UpdateGooglePlayListingInput = {
   packageName: string;
   language: string;
@@ -481,6 +511,179 @@ function stringOrNull(value: unknown): string | null {
   return normalized.length > 0 ? normalized : null;
 }
 
+function normalizeLocaleTag(locale: string): string {
+  return String(locale || '')
+    .trim()
+    .replace(/_/g, '-')
+    .toLowerCase();
+}
+
+function localeLanguage(locale: string): string {
+  const normalized = normalizeLocaleTag(locale);
+  if (!normalized) return '';
+  return normalized.split('-')[0] || '';
+}
+
+function findBestLocaleMatch(candidates: string[], targetLocale: string): string | null {
+  const normalizedTarget = normalizeLocaleTag(targetLocale);
+  if (!normalizedTarget) return null;
+
+  const exact = candidates.find((candidate) => normalizeLocaleTag(candidate) === normalizedTarget);
+  if (exact) return exact;
+
+  const lang = localeLanguage(targetLocale);
+  if (!lang) return null;
+
+  const sameLanguage = candidates.find((candidate) => localeLanguage(candidate) === lang);
+  return sameLanguage || null;
+}
+
+function mapAppleLocaleToGoogleLocale(locale: string): string {
+  const normalized = normalizeLocaleTag(locale);
+  if (!normalized) return 'en-US';
+
+  const map: Record<string, string> = {
+    en: 'en-US',
+    'en-us': 'en-US',
+    es: 'es-ES',
+    'es-es': 'es-ES',
+    fr: 'fr-FR',
+    'fr-fr': 'fr-FR',
+    de: 'de-DE',
+    'de-de': 'de-DE',
+    it: 'it-IT',
+    'it-it': 'it-IT',
+    pt: 'pt-PT',
+    'pt-br': 'pt-BR',
+    ru: 'ru-RU',
+    'ru-ru': 'ru-RU',
+    hi: 'hi-IN',
+    'hi-in': 'hi-IN',
+    ja: 'ja-JP',
+    'ja-jp': 'ja-JP',
+    ko: 'ko-KR',
+    'ko-kr': 'ko-KR',
+    'zh-hans': 'zh-CN',
+    'zh-hans-cn': 'zh-CN',
+    'zh-cn': 'zh-CN',
+    'zh-hant': 'zh-TW',
+    'zh-hant-tw': 'zh-TW',
+    'zh-tw': 'zh-TW',
+  };
+
+  if (map[normalized]) return map[normalized];
+  return locale.replace(/_/g, '-');
+}
+
+const APPLE_VERSION_STATE_PRIORITY: Record<string, number> = {
+  PREPARE_FOR_SUBMISSION: 100,
+  DEVELOPER_REJECTED: 95,
+  METADATA_REJECTED: 90,
+  REJECTED: 85,
+  WAITING_FOR_REVIEW: 80,
+  IN_REVIEW: 70,
+  PENDING_DEVELOPER_RELEASE: 60,
+  PENDING_APPLE_RELEASE: 55,
+  PROCESSING_FOR_DISTRIBUTION: 50,
+  READY_FOR_DISTRIBUTION: 45,
+};
+
+function rankAppleVersionState(state: string | null | undefined): number {
+  if (!state) return 0;
+  return APPLE_VERSION_STATE_PRIORITY[state.toUpperCase()] || 0;
+}
+
+function compareVersionStrings(left: string | null | undefined, right: string | null | undefined): number {
+  const leftParts = String(left || '')
+    .split('.')
+    .map((part) => Number.parseInt(part, 10))
+    .filter((value) => Number.isFinite(value));
+  const rightParts = String(right || '')
+    .split('.')
+    .map((part) => Number.parseInt(part, 10))
+    .filter((value) => Number.isFinite(value));
+
+  const maxLen = Math.max(leftParts.length, rightParts.length);
+  for (let i = 0; i < maxLen; i += 1) {
+    const leftValue = leftParts[i] || 0;
+    const rightValue = rightParts[i] || 0;
+    if (leftValue > rightValue) return 1;
+    if (leftValue < rightValue) return -1;
+  }
+
+  return 0;
+}
+
+function compareAppleVersionLocalizations(
+  left: AppleVersionLocalization,
+  right: AppleVersionLocalization
+): number {
+  const stateRankDiff = rankAppleVersionState(left.appStoreState) - rankAppleVersionState(right.appStoreState);
+  if (stateRankDiff !== 0) return stateRankDiff;
+
+  const versionDiff = compareVersionStrings(left.versionString, right.versionString);
+  if (versionDiff !== 0) return versionDiff;
+
+  const leftCopyScore =
+    (left.description?.length || 0) +
+    (left.whatsNew?.length || 0) +
+    (left.promotionalText?.length || 0);
+  const rightCopyScore =
+    (right.description?.length || 0) +
+    (right.whatsNew?.length || 0) +
+    (right.promotionalText?.length || 0);
+  if (leftCopyScore !== rightCopyScore) {
+    return leftCopyScore - rightCopyScore;
+  }
+
+  return 0;
+}
+
+function dedupeAppleVersionLocalizations(
+  localizations: AppleVersionLocalization[]
+): AppleVersionLocalization[] {
+  const bestByLocale = new Map<string, AppleVersionLocalization>();
+
+  for (const localization of localizations) {
+    const normalizedLocale = normalizeLocaleTag(localization.locale);
+    if (!normalizedLocale) continue;
+
+    const current = bestByLocale.get(normalizedLocale);
+    if (!current || compareAppleVersionLocalizations(localization, current) > 0) {
+      bestByLocale.set(normalizedLocale, localization);
+    }
+  }
+
+  return Array.from(bestByLocale.values()).sort((a, b) => a.locale.localeCompare(b.locale));
+}
+
+function pickPreferredAppleVersion(versions: JsonApiResource[]): JsonApiResource | null {
+  if (!versions.length) return null;
+
+  const sorted = [...versions].sort((left, right) => {
+    const stateRankDiff =
+      rankAppleVersionState(stringOrNull(right.attributes?.appStoreState)) -
+      rankAppleVersionState(stringOrNull(left.attributes?.appStoreState));
+    if (stateRankDiff !== 0) return stateRankDiff;
+
+    const versionDiff = compareVersionStrings(
+      stringOrNull(right.attributes?.versionString),
+      stringOrNull(left.attributes?.versionString)
+    );
+    if (versionDiff !== 0) return versionDiff;
+
+    const rightCreated = Date.parse(String(right.attributes?.createdDate || ''));
+    const leftCreated = Date.parse(String(left.attributes?.createdDate || ''));
+    if (Number.isFinite(rightCreated) && Number.isFinite(leftCreated) && rightCreated !== leftCreated) {
+      return rightCreated - leftCreated;
+    }
+
+    return 0;
+  });
+
+  return sorted[0] || null;
+}
+
 function removeUndefinedFields<T extends Record<string, unknown>>(attributes: T): Partial<T> {
   const output: Partial<T> = {};
   for (const [key, value] of Object.entries(attributes)) {
@@ -569,11 +772,75 @@ async function fetchApplePublishingSnapshot(bundleId: string): Promise<AppleStor
     }
   }
 
+  const appInfoLocales = Array.from(
+    new Set(
+      appInfoLocalizations
+        .map((row) => row.locale)
+        .filter((locale): locale is string => Boolean(locale))
+    )
+  ).sort((a, b) => a.localeCompare(b));
+
+  const versionLocales = Array.from(
+    new Set(
+      versionLocalizations
+        .map((row) => row.locale)
+        .filter((locale): locale is string => Boolean(locale))
+    )
+  ).sort((a, b) => a.localeCompare(b));
+
+  const mappedVersionLocaleByAppInfoLocale: Record<string, string | null> = {};
+  for (const appInfoLocale of appInfoLocales) {
+    mappedVersionLocaleByAppInfoLocale[appInfoLocale] = findBestLocaleMatch(versionLocales, appInfoLocale);
+  }
+
+  const missingVersionLocales = appInfoLocales.filter(
+    (locale) => !mappedVersionLocaleByAppInfoLocale[locale]
+  );
+
+  const versionScreenshotSummaryByLocalizationId: Record<string, AppleLocalizationScreenshotSummary> = {};
+  for (const localization of versionLocalizations) {
+    const byDisplayType: Record<string, { total: number; complete: number }> = {};
+    let total = 0;
+    let complete = 0;
+
+    try {
+      const sets = await fetchAppleScreenshotSetsWithClient(client, localization.id);
+      for (const set of sets) {
+        const screenshots = Array.isArray(set.screenshots) ? set.screenshots : [];
+        const setTotal = screenshots.length;
+        const setComplete = screenshots.filter((s) => s.assetState === 'COMPLETE').length;
+        byDisplayType[set.displayType] = {
+          total: setTotal,
+          complete: setComplete,
+        };
+        total += setTotal;
+        complete += setComplete;
+      }
+    } catch {
+      // Keep localization row visible even if screenshot summary fails for that locale.
+    }
+
+    versionScreenshotSummaryByLocalizationId[localization.id] = {
+      localizationId: localization.id,
+      locale: localization.locale,
+      total,
+      complete,
+      byDisplayType,
+    };
+  }
+
   return {
     bundleId,
     appId,
     appInfoLocalizations,
     versionLocalizations,
+    versionScreenshotSummaryByLocalizationId,
+    localeCoverage: {
+      appInfoLocales,
+      versionLocales,
+      missingVersionLocales,
+      mappedVersionLocaleByAppInfoLocale,
+    },
   };
 }
 
@@ -587,7 +854,23 @@ async function createGoogleEdit(client: AxiosInstance, packageName: string): Pro
 }
 
 async function commitGoogleEdit(client: AxiosInstance, packageName: string, editId: string): Promise<void> {
-  await client.post(`/androidpublisher/v3/applications/${packageName}/edits/${editId}:commit`);
+  const path = `/androidpublisher/v3/applications/${packageName}/edits/${editId}:commit`;
+  try {
+    await client.post(path, {}, { params: { changesNotSentForReview: true } });
+  } catch (error) {
+    if (isAxiosError(error) && error.response?.status === 400) {
+      const detail = JSON.stringify(error.response.data || {}).toLowerCase();
+      if (
+        detail.includes('changesnotsentforreview') ||
+        detail.includes('unknown query parameter') ||
+        detail.includes('invalid value at')
+      ) {
+        await client.post(path);
+        return;
+      }
+    }
+    throw error;
+  }
 }
 
 async function deleteGoogleEdit(client: AxiosInstance, packageName: string, editId: string): Promise<void> {
@@ -828,6 +1111,153 @@ export async function updateAppleVersionLocalization(
   };
 }
 
+export async function syncAppleVersionLocalizationsFromAppInfo(
+  bundleId?: string
+): Promise<SyncAppleVersionLocalizationsResult> {
+  const resolvedBundleId = bundleId || (await resolveStoreIds({})).bundleId;
+  if (!resolvedBundleId) {
+    throw new Error('No Apple bundle ID configured.');
+  }
+
+  const client = await createAppleClient();
+  const appId = await findAppleAppIdByBundleId(client, resolvedBundleId);
+
+  const appInfos = await appleGetAll(client, `/v1/apps/${appId}/appInfos`, {
+    'fields[appInfos]': 'state',
+    limit: 50,
+  });
+
+  const sourceAppInfoLocaleSet = new Set<string>();
+  for (const appInfo of appInfos.data) {
+    const localizations = await appleGetAll(client, `/v1/appInfos/${appInfo.id}/appInfoLocalizations`, {
+      'fields[appInfoLocalizations]': 'locale',
+      limit: 200,
+    });
+    for (const localization of localizations.data) {
+      const locale = String(localization.attributes?.locale || '').trim();
+      if (locale) {
+        sourceAppInfoLocaleSet.add(locale);
+      }
+    }
+  }
+
+  const sourceAppInfoLocales = Array.from(sourceAppInfoLocaleSet).sort((a, b) => a.localeCompare(b));
+  if (!sourceAppInfoLocales.length) {
+    throw new Error(`No app-info localizations were found for bundle ID "${resolvedBundleId}".`);
+  }
+
+  const versions = await appleGetAll(client, `/v1/apps/${appId}/appStoreVersions`, {
+    'filter[platform]': 'IOS',
+    'fields[appStoreVersions]': 'versionString,appStoreState,createdDate,platform',
+    limit: 200,
+  });
+
+  const targetVersion = pickPreferredAppleVersion(versions.data);
+  if (!targetVersion) {
+    throw new Error(`No iOS App Store version found for bundle ID "${resolvedBundleId}".`);
+  }
+
+  const targetVersionId = targetVersion.id;
+  const targetVersionString = stringOrNull(targetVersion.attributes?.versionString);
+  const targetVersionState = stringOrNull(targetVersion.attributes?.appStoreState);
+
+  const existingLocalizations = await appleGetAll(
+    client,
+    `/v1/appStoreVersions/${targetVersionId}/appStoreVersionLocalizations`,
+    {
+      'fields[appStoreVersionLocalizations]': 'locale,description,keywords,promotionalText,marketingUrl,supportUrl,whatsNew',
+      limit: 200,
+    }
+  );
+
+  const existingLocaleSet = new Set<string>();
+  for (const localization of existingLocalizations.data) {
+    const locale = String(localization.attributes?.locale || '').trim();
+    if (locale) existingLocaleSet.add(locale);
+  }
+
+  const skippedExistingSet = new Set<string>();
+  const skippedFailed: Array<{ locale: string; reason: string }> = [];
+  const created: AppleVersionLocalization[] = [];
+
+  for (const appInfoLocale of sourceAppInfoLocales) {
+    const existingLocales = Array.from(existingLocaleSet);
+    const matched = findBestLocaleMatch(existingLocales, appInfoLocale);
+    if (matched) {
+      skippedExistingSet.add(appInfoLocale);
+      continue;
+    }
+
+    try {
+      const response = await client.post<JsonApiSingleResponse>('/v1/appStoreVersionLocalizations', {
+        data: {
+          type: 'appStoreVersionLocalizations',
+          attributes: {
+            locale: appInfoLocale,
+          },
+          relationships: {
+            appStoreVersion: {
+              data: { type: 'appStoreVersions', id: targetVersionId },
+            },
+          },
+        },
+      });
+
+      const item = response.data.data;
+      const createdLocale = String(item.attributes?.locale || appInfoLocale);
+      existingLocaleSet.add(createdLocale);
+      created.push({
+        id: item.id,
+        appStoreVersionId: targetVersionId,
+        appStoreState: targetVersionState,
+        versionString: targetVersionString,
+        locale: createdLocale,
+        description: stringOrNull(item.attributes?.description),
+        keywords: stringOrNull(item.attributes?.keywords),
+        promotionalText: stringOrNull(item.attributes?.promotionalText),
+        marketingUrl: stringOrNull(item.attributes?.marketingUrl),
+        supportUrl: stringOrNull(item.attributes?.supportUrl),
+        whatsNew: stringOrNull(item.attributes?.whatsNew),
+      });
+    } catch (error) {
+      if (isAxiosError(error) && error.response?.status === 409) {
+        skippedExistingSet.add(appInfoLocale);
+        continue;
+      }
+
+      if (isAxiosError(error)) {
+        const status = error.response?.status;
+        const detail = error.response?.data
+          ? JSON.stringify(error.response.data).slice(0, 300)
+          : error.message;
+        skippedFailed.push({
+          locale: appInfoLocale,
+          reason: status ? `Apple API ${status}: ${detail}` : detail,
+        });
+        continue;
+      }
+
+      skippedFailed.push({
+        locale: appInfoLocale,
+        reason: error instanceof Error ? error.message : String(error),
+      });
+    }
+  }
+
+  return {
+    bundleId: resolvedBundleId,
+    appId,
+    targetVersionId,
+    targetVersionString,
+    targetVersionState,
+    sourceAppInfoLocales,
+    existingVersionLocales: Array.from(existingLocaleSet).sort((a, b) => a.localeCompare(b)),
+    created,
+    skippedExisting: Array.from(skippedExistingSet).sort((a, b) => a.localeCompare(b)),
+    skippedFailed,
+  };
+}
+
 export async function updateGooglePlayListing(
   input: UpdateGooglePlayListingInput
 ): Promise<GooglePlayListing> {
@@ -898,9 +1328,10 @@ function resolveAppleTemplateUrl(templateUrl: string, width: number, height: num
     .replace('{f}', 'png');
 }
 
-export async function fetchAppleScreenshotSets(localizationId: string): Promise<AppleScreenshotSet[]> {
-  const client = await createAppleClient();
-
+async function fetchAppleScreenshotSetsWithClient(
+  client: AxiosInstance,
+  localizationId: string
+): Promise<AppleScreenshotSet[]> {
   const setsResult = await appleGetAll(
     client,
     `/v1/appStoreVersionLocalizations/${localizationId}/appScreenshotSets`,
@@ -946,6 +1377,11 @@ export async function fetchAppleScreenshotSets(localizationId: string): Promise<
   return sets;
 }
 
+export async function fetchAppleScreenshotSets(localizationId: string): Promise<AppleScreenshotSet[]> {
+  const client = await createAppleClient();
+  return fetchAppleScreenshotSetsWithClient(client, localizationId);
+}
+
 export async function uploadAppleScreenshot(input: UploadAppleScreenshotInput): Promise<AppleScreenshot> {
   const client = await createAppleClient();
 
@@ -1169,14 +1605,43 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
     throw new Error('Could not fetch store snapshots. Check API credentials.');
   }
 
+  const appInfoRows = apple.appInfoLocalizations || [];
+  const appInfoLocales = appInfoRows
+    .map((row) => row.locale)
+    .filter((locale): locale is string => Boolean(locale));
+  const appInfoByNormalizedLocale = new Map<string, AppleAppInfoLocalization>();
+  for (const row of appInfoRows) {
+    const key = normalizeLocaleTag(row.locale);
+    if (!key) continue;
+    const current = appInfoByNormalizedLocale.get(key);
+    const currentScore = (current?.name?.length || 0) + (current?.subtitle?.length || 0);
+    const nextScore = (row.name?.length || 0) + (row.subtitle?.length || 0);
+    if (!current || nextScore > currentScore) {
+      appInfoByNormalizedLocale.set(key, row);
+    }
+  }
+
+  const dedupedAppleVersionRows = dedupeAppleVersionLocalizations(apple.versionLocalizations || []);
+  const appleVersionByGoogleLocale = new Map<string, AppleVersionLocalization>();
+  for (const localization of dedupedAppleVersionRows) {
+    const googleLocale = mapAppleLocaleToGoogleLocale(localization.locale);
+    const current = appleVersionByGoogleLocale.get(googleLocale);
+    if (!current || compareAppleVersionLocalizations(localization, current) > 0) {
+      appleVersionByGoogleLocale.set(googleLocale, localization);
+    }
+  }
+
   // 2. Sync texts: Apple version localizations → Google listings
-  for (const appleLoc of apple.versionLocalizations || []) {
-    const locale = appleLoc.locale; // e.g. "en-US"
+  for (const [googleLocale, appleLoc] of appleVersionByGoogleLocale) {
+    const locale = appleLoc.locale;
     const description = appleLoc.description || '';
     if (!description) continue;
 
     // Find matching Apple app info localization for title/subtitle
-    const appInfo = (apple.appInfoLocalizations || []).find(a => a.locale === locale);
+    const appInfoLocaleMatch = findBestLocaleMatch(appInfoLocales, locale);
+    const appInfo = appInfoLocaleMatch
+      ? appInfoByNormalizedLocale.get(normalizeLocaleTag(appInfoLocaleMatch))
+      : undefined;
     const title = appInfo?.name || '';
     const subtitle = appInfo?.subtitle || '';
 
@@ -1188,15 +1653,15 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
     try {
       await updateGooglePlayListing({
         packageName: google.packageName,
-        language: locale,
+        language: googleLocale,
         title: finalTitle,
         shortDescription,
         fullDescription,
       });
-      result.textsSynced.push(locale);
+      result.textsSynced.push(locale === googleLocale ? locale : `${locale} -> ${googleLocale}`);
     } catch (err: any) {
       const detail = err.response?.data ? JSON.stringify(err.response.data).slice(0, 300) : err.message;
-      result.errors.push(`Text sync ${locale}: ${detail}`);
+      result.errors.push(`Text sync ${locale} -> ${googleLocale}: ${detail}`);
     }
   }
 
@@ -1213,7 +1678,7 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
     'APP_IPAD_97': 'sevenInchScreenshots',
   };
 
-  for (const appleLoc of apple.versionLocalizations || []) {
+  for (const [googleLocale, appleLoc] of appleVersionByGoogleLocale) {
     const locale = appleLoc.locale;
     const localizationId = appleLoc.id;
 
@@ -1221,7 +1686,7 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
     try {
       screenshotSets = await fetchAppleScreenshotSets(localizationId);
     } catch (err: any) {
-      result.errors.push(`Fetch screenshots ${locale}: ${err.message}`);
+      result.errors.push(`Fetch screenshots ${locale} -> ${googleLocale}: ${err.message}`);
       continue;
     }
 
@@ -1239,7 +1704,7 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
 
       // Delete existing Google images of this type first
       try {
-        await deleteAllGoogleImages(google.packageName, locale, googleType as GooglePlayImageType);
+        await deleteAllGoogleImages(google.packageName, googleLocale, googleType as GooglePlayImageType);
       } catch {
         // May fail if no images exist — that's fine
       }
@@ -1264,7 +1729,7 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
 
           await uploadGoogleImage({
             packageName: google.packageName,
-            language: locale,
+            language: googleLocale,
             imageType: googleType as GooglePlayImageType,
             fileName: ss.fileName || 'screenshot.png',
             fileData,
@@ -1273,7 +1738,7 @@ export async function syncAppleToGoogle(): Promise<SyncAppleToGoogleResult> {
           result.imagesSynced++;
         } catch (err: any) {
           const detail = err.response?.data ? JSON.stringify(err.response.data).slice(0, 300) : err.message;
-          result.errors.push(`Image upload ${locale}/${googleType}/${ss.fileName}: ${detail}`);
+          result.errors.push(`Image upload ${locale} -> ${googleLocale}/${googleType}/${ss.fileName}: ${detail}`);
         }
       }
     }
diff --git a/1-in-a-billion-v2/backend/src/workers/pdfWorker.ts b/1-in-a-billion-v2/backend/src/workers/pdfWorker.ts
index 711b55b8..5bdbdce1 100755
--- a/1-in-a-billion-v2/backend/src/workers/pdfWorker.ts
+++ b/1-in-a-billion-v2/backend/src/workers/pdfWorker.ts
@@ -118,6 +118,10 @@ export class PdfWorker extends BaseWorker {
     const person2 = params.person2 || undefined;
     let person1Id: string | undefined = person1?.id;
     let person2Id: string | undefined = person2?.id;
+    const getJobPortraitUrl = (key: 'person1' | 'person2'): string | null => {
+      const value = params?._portraitUrls?.[key];
+      return typeof value === 'string' && value.trim().length > 0 ? value : null;
+    };
 
     const getPortraitUrl = async (clientPersonId?: string, personName?: string, isSelfReading?: boolean): Promise<{ url: string | null; id: string | null }> => {
       // Try by client_person_id first
@@ -198,49 +202,63 @@ export class PdfWorker extends BaseWorker {
       return null;
     };
 
-    // If portraits are still being generated, wait briefly so PDFs can include them.
-    // This prevents "empty image" PDFs right after a new photo upload.
-    const maxWaitMs = 60_000;
-    const pollMs = 3_000;
-    const startedAt = Date.now();
-
     let person1PortraitUrl: string | null = null;
     let person2PortraitUrl: string | null = null;
     let existingCoupleImageUrl: string | null = null;
 
     const isPerson2ReadingLocal = docType === 'person2';
     const isOverlayReadingLocal = docType === 'overlay' || docType === 'verdict';
+    const isMultiPersonJob = Boolean(person2);
+
+    if (isMultiPersonJob) {
+      // Strict mode: synastry/multi-person jobs must use per-job portrait URLs.
+      // No fallback to profile rows or avatars.
+      person1PortraitUrl = getJobPortraitUrl('person1');
+      person2PortraitUrl = getJobPortraitUrl('person2');
+
+      if (!person1PortraitUrl || !person2PortraitUrl) {
+        return {
+          success: false,
+          error: 'Missing required job portrait URLs (_portraitUrls.person1/person2) for multi-person PDF generation',
+        };
+      }
 
-    while (Date.now() - startedAt < maxWaitMs) {
-      const [p1Result, p2Result] = await Promise.all([
-        getPortraitUrl(person1Id, person1?.name, !!person1?.is_user),
-        getPortraitUrl(person2Id, person2?.name, !!person2?.is_user),
-      ]);
-
-      person1PortraitUrl = p1Result.url;
-      person2PortraitUrl = p2Result.url;
-
-      // Update person IDs if we found them by name
-      if (!person1Id && p1Result.id) person1Id = p1Result.id;
-      if (!person2Id && p2Result.id) person2Id = p2Result.id;
-
-      // For single PDFs we only need p1. For overlay PDFs we want p1+p2.
-      const hasSingleReady = !!person1PortraitUrl;
-      const hasOverlayReady = !person2 || (!!person1PortraitUrl && !!person2PortraitUrl);
-      if (hasSingleReady && hasOverlayReady) break;
-
-      await sleep(pollMs);
-    }
+      console.log('🖼️ [PDFWorker] Using strict per-job portrait URLs from params._portraitUrls');
+    } else {
+      // If portraits are still being generated, wait briefly so PDFs can include them.
+      // This prevents "empty image" PDFs right after a new photo upload.
+      const maxWaitMs = 60_000;
+      const pollMs = 3_000;
+      const startedAt = Date.now();
+
+      while (Date.now() - startedAt < maxWaitMs) {
+        const [p1Result, p2Result] = await Promise.all([
+          getPortraitUrl(person1Id, person1?.name, !!person1?.is_user),
+          getPortraitUrl(person2Id, person2?.name, !!person2?.is_user),
+        ]);
+
+        person1PortraitUrl = p1Result.url;
+        person2PortraitUrl = p2Result.url;
+
+        // Update person IDs if we found them by name
+        if (!person1Id && p1Result.id) person1Id = p1Result.id;
+        if (!person2Id && p2Result.id) person2Id = p2Result.id;
+
+        // For single PDFs we only need p1. For overlay PDFs we want p1+p2.
+        const hasSingleReady = !!person1PortraitUrl;
+        const hasOverlayReady = !person2 || (!!person1PortraitUrl && !!person2PortraitUrl);
+        if (hasSingleReady && hasOverlayReady) break;
+
+        await sleep(pollMs);
+      }
 
-    // ── FALLBACK FOR MISSING PORTRAITS ──
-    // If after the timeout (or immediately if they didn't have one in DB) we still lack a portrait, 
-    // assign the faceless placeholder avatar based on their personId.
-    if (!person1PortraitUrl && person1Id) {
-      person1PortraitUrl = getAvatarFileUrl(person1Id);
-    }
-    // Only assign a fallback to Person 2 if the document actually needs Person 2 shown
-    if ((isPerson2ReadingLocal || isOverlayReadingLocal) && !person2PortraitUrl && person2Id) {
-      person2PortraitUrl = getAvatarFileUrl(person2Id);
+      // Fallback for single-person jobs only.
+      if (!person1PortraitUrl && person1Id) {
+        person1PortraitUrl = getAvatarFileUrl(person1Id);
+      }
+      if ((isPerson2ReadingLocal || isOverlayReadingLocal) && !person2PortraitUrl && person2Id) {
+        person2PortraitUrl = getAvatarFileUrl(person2Id);
+      }
     }
 
     // Skip attempting to fetch an existing cached couple image.
@@ -252,8 +270,8 @@ export class PdfWorker extends BaseWorker {
     if (isOverlayReadingLocal && person1PortraitUrl && person2PortraitUrl && person1Id && person2Id) {
       // ⚠️ CRITICAL: Ensure we're using styled portraits, not original photos
       // getPortraitUrl() prefers portrait_url, but falls back to original_photo_url
-      const isStyled1 = person1PortraitUrl.includes('/AI-generated-portrait.png');
-      const isStyled2 = person2PortraitUrl.includes('/AI-generated-portrait.png');
+      const isStyled1 = person1PortraitUrl.includes('/AI-generated-portrait');
+      const isStyled2 = person2PortraitUrl.includes('/AI-generated-portrait');
 
       if (!isStyled1 || !isStyled2) {
         console.warn('⚠️ [PDFWorker] WARNING: Portrait URLs appear to be original photos, not styled portraits!');
diff --git a/1-in-a-billion-v2/backend/src/workers/textWorker.ts b/1-in-a-billion-v2/backend/src/workers/textWorker.ts
index e48c24d4..afe764f9 100755
--- a/1-in-a-billion-v2/backend/src/workers/textWorker.ts
+++ b/1-in-a-billion-v2/backend/src/workers/textWorker.ts
@@ -168,6 +168,71 @@ function scoreToQualitative(score: number): string {
   return 'MINIMAL';
 }
 
+type SynastryLikeCategory = { key?: string; label?: string; score?: number };
+type SynastryLikeResult = { categories?: SynastryLikeCategory[] };
+
+function getCategoryScore(result: SynastryLikeResult | null | undefined, categoryKey: string): number | null {
+  const keyUpper = String(categoryKey || '').toUpperCase();
+  const rows = Array.isArray(result?.categories) ? result!.categories! : [];
+  const byKey = rows.find((row) => String(row?.key || '').toUpperCase() === keyUpper);
+  if (Number.isFinite(byKey?.score)) return Number(byKey!.score);
+
+  // Fallback by label for engines that might not provide stable keys.
+  if (keyUpper === 'SEXUAL_CHEMISTRY') {
+    const byLabel = rows.find((row) => /sexual\s*chemistry/i.test(String(row?.label || '')));
+    if (Number.isFinite(byLabel?.score)) return Number(byLabel!.score);
+  }
+  return null;
+}
+
+function buildKabbalahSexConsensusGuardrail(params: {
+  kabScores: SynastryLikeResult;
+  p1Placements: any;
+  p2Placements: any;
+}): string {
+  try {
+    const kabSex = getCategoryScore(params.kabScores, 'SEXUAL_CHEMISTRY');
+    if (kabSex == null) return '';
+
+    const peerResults: Array<{ name: string; result: SynastryLikeResult }> = [
+      { name: 'Western', result: computeWesternSynastry(params.p1Placements, params.p2Placements) as SynastryLikeResult },
+      { name: 'Human Design', result: computeHDSynastry(params.p1Placements, params.p2Placements) as SynastryLikeResult },
+      { name: 'Gene Keys', result: computeGeneKeysSynastry(params.p1Placements, params.p2Placements) as SynastryLikeResult },
+    ];
+
+    const peers = peerResults
+      .map((p) => ({ ...p, sex: getCategoryScore(p.result, 'SEXUAL_CHEMISTRY') }))
+      .filter((p): p is { name: string; result: SynastryLikeResult; sex: number } => p.sex != null);
+
+    if (peers.length < 2) return '';
+
+    const highPeers = peers.filter((p) => p.sex >= 60);
+    const peerAverage = Math.round(peers.reduce((sum, p) => sum + p.sex, 0) / peers.length);
+
+    // Global contradiction guardrail:
+    // If Kabbalah looks low while peers are consistently high, force interpretation
+    // toward "high charge + blocked container" instead of "no attraction".
+    const contradiction = kabSex <= 35 && highPeers.length >= 2 && peerAverage >= 60;
+    if (!contradiction) return '';
+
+    const peerSummary = peers
+      .map((p) => `${p.name}: ${scoreToQualitative(p.sex)}`)
+      .join(' | ');
+
+    return [
+      '',
+      'CROSS-SYSTEM CONSENSUS NOTE (interpretive constraint; do not quote this note verbatim):',
+      `- Peer sexual chemistry signals: ${peerSummary}.`,
+      `- Kabbalah sexual chemistry signal: ${scoreToQualitative(kabSex)}.`,
+      '- Interpretation rule: Do NOT frame this pair as low-attraction.',
+      '- If Kabbalah is low while peers are high, read this as blocked vessel / containment friction, not absent erotic pull.',
+      '- Write both truths together: strong charge + structural/spiritual correction pressure.',
+    ].join('\n');
+  } catch {
+    return '';
+  }
+}
+
 /**
  * Compact voice DNA for expansion passes.
  * The full writing prompt has ~2000 tokens of literary guidance. Expansion passes
@@ -2056,6 +2121,17 @@ Do not recompute or contradict them.
             combinedChartData += deterministicBlock;
             chartDataForPrompt += deterministicBlock;
             console.log(`📊 [TextWorker] Kabbalah deterministic synastry scores injected (overall: ${kabScores.overallScore})`);
+
+            const consensusGuardrail = buildKabbalahSexConsensusGuardrail({
+              kabScores,
+              p1Placements,
+              p2Placements,
+            });
+            if (consensusGuardrail) {
+              combinedChartData += `\n\n${consensusGuardrail}`;
+              chartDataForPrompt += `\n\n${consensusGuardrail}`;
+              console.log('🧭 [TextWorker] Kabbalah cross-system contradiction guardrail injected');
+            }
           }
         } catch (err: any) {
           console.warn(`⚠️ [TextWorker] Kabbalah synastry scoring failed, continuing without: ${err?.message}`);
@@ -2112,18 +2188,19 @@ Do not recompute or contradict them.
     const langCode = params.outputLanguage || 'en';
     const llmSystemPrompt: string | undefined = (() => {
       const englishGuardrail = 'OUTPUT LANGUAGE: English. Write the ENTIRE output in English. Do NOT switch to any other language at any point — even if the prompt mentions foreign authors or languages. All prose, all terminology, every word must be in English.';
-      if (langCode === 'en') return englishGuardrail;
-      if (!isValidLanguage(langCode)) return englishGuardrail;
-      const langInstruction = getLanguageInstruction(langCode as OutputLanguage);
-      return [
-        langInstruction,
-        '',
-        'LAYER 3 REWRITE CONTEXT:',
+      const rewriteContext = [
+        'LAYER 3 REWRITE CONTEXT (HYBRID):',
         '- This is a literary transformation pass, not a fresh diagnostic analysis.',
         '- Keep chart truths and emotional intensity intact.',
-        '- Use system terms as anchors when useful, but keep most prose in lived human language.',
-        '- Explicit language is allowed when it carries truth.',
+        '- Keep system terms as anchors when useful, then decode into modern relational language.',
+        '- Translate only the high-voltage points; do not flatten every line into jargon.',
+        '- Explicit labels are welcome when intensity is high.',
+        '- Never moralize or shame desire.',
       ].join('\n');
+      if (langCode === 'en') return [englishGuardrail, '', rewriteContext].join('\n');
+      if (!isValidLanguage(langCode)) return [englishGuardrail, '', rewriteContext].join('\n');
+      const langInstruction = getLanguageInstruction(langCode as OutputLanguage);
+      return [langInstruction, '', rewriteContext].join('\n');
     })();
     let extractedFooter = { body: '', footer: '' };
     // Headline inference is disabled globally for stable plain-prose output.
@@ -2386,6 +2463,16 @@ Do not recompute or contradict them.
         '- Keep all factual conclusions and emotional truth; transform the language into stronger prose.',
       ].join('\n');
 
+      const PSYCHOSEXUAL_TRANSLATION_MANDATE = [
+        'PSYCHOSEXUAL TRANSLATION MANDATE (LAYER 3):',
+        '- After naming one system anchor once, translate into modern human language. No technical repetition.',
+        '- Soft rule: if a motif repeats three or more times in the analysis, usually treat it as a primary driver unless evidence is weak.',
+        '- Allowed labels (use directly when they fit): compulsive erotic drive (or nymphomaniac drive), submissive behavior pattern, dominance-seeking pattern, fetishized power asymmetry, humiliation/validation loop, avoidant-intimacy split, obsessional attachment loop.',
+        '- For each primary driver, make sure the prose shows: the label, what they actually do, the relational cost, and what conscious expression looks like (consensual, ethical, contained).',
+        '- Use phrasing like "they function best when this drive is consciously structured" instead of lifestyle prescriptions.',
+        '- Do not sanitize. Do not moralize. Do not hide behind system jargon.',
+      ].join('\n');
+
       const rewritePrompt = systemRewriteInstructions
         ? [
             systemRewriteInstructions,
@@ -2394,6 +2481,8 @@ Do not recompute or contradict them.
             '',
             NARRATIVE_TRANSFORMATION_GUARDRAIL,
             '',
+            PSYCHOSEXUAL_TRANSLATION_MANDATE,
+            '',
             'PERSONAL QUESTION HANDLING:',
             'If the reading below contains a section marked "═══ PERSONAL QUESTION ANALYSIS ═══",',
             'rewrite it in your literary voice and place it as the OPENING of your essay — the very first page.',
@@ -2416,6 +2505,8 @@ Do not recompute or contradict them.
             '',
             NARRATIVE_TRANSFORMATION_GUARDRAIL,
             '',
+            PSYCHOSEXUAL_TRANSLATION_MANDATE,
+            '',
             'READING TO REWRITE:',
             initial,
           ].join('\n');
@@ -2434,26 +2525,30 @@ Do not recompute or contradict them.
       );
     }
 
-    await updateTextProgress('rewriting', `🔄 Rewriting ${title}...`, 0.65);
-    console.log(`🔄 [TextWorker] Running narrative rewrite pass (${wordCount} words input)...`);
-    text = await narrativeRewrite(text);
-    extractedFooter = extractChartSignatureFooter(text);
-    text = extractedFooter.body;
-    wordCount = countWords(text);
-    console.log(`✅ [TextWorker] Narrative rewrite complete: ${wordCount} words`);
-
-    text = await rewriteIncarnationIfNeeded(text);
-    // Re-extract footer after rewrite so it cannot remain mid-document.
-    extractedFooter = extractChartSignatureFooter(text);
-    text = extractedFooter.body;
-    wordCount = countWords(text);
-
-    // Rewrite passes can reduce total length; top up again before final compliance/floor checks.
-    if (wordCount < HARD_FLOOR_WORDS) {
-      text = await expandToHardFloor(text);
+    if (docType !== 'verdict') {
+      await updateTextProgress('rewriting', `🔄 Rewriting ${title}...`, 0.65);
+      console.log(`🔄 [TextWorker] Running narrative rewrite pass (${wordCount} words input)...`);
+      text = await narrativeRewrite(text);
+      extractedFooter = extractChartSignatureFooter(text);
+      text = extractedFooter.body;
+      wordCount = countWords(text);
+      console.log(`✅ [TextWorker] Narrative rewrite complete: ${wordCount} words`);
+
+      text = await rewriteIncarnationIfNeeded(text);
+      // Re-extract footer after rewrite so it cannot remain mid-document.
       extractedFooter = extractChartSignatureFooter(text);
       text = extractedFooter.body;
       wordCount = countWords(text);
+
+      // Rewrite passes can reduce total length; top up again before final compliance/floor checks.
+      if (wordCount < HARD_FLOOR_WORDS) {
+        text = await expandToHardFloor(text);
+        extractedFooter = extractChartSignatureFooter(text);
+        text = extractedFooter.body;
+        wordCount = countWords(text);
+      }
+    } else {
+      console.log('⏭️ [TextWorker] Skipping narrative rewrite pass for verdict by policy.');
     }
 
     if (preserveSurrealHeadlines) {
diff --git a/1-in-a-billion-v2/backend/tmp_store_audit.ts b/1-in-a-billion-v2/backend/tmp_store_audit.ts
new file mode 100644
index 00000000..55f6fcda
--- /dev/null
+++ b/1-in-a-billion-v2/backend/tmp_store_audit.ts
@@ -0,0 +1,57 @@
+import 'dotenv/config';
+import {
+  fetchStorePublishingSnapshot,
+  fetchAppleScreenshotSets,
+  syncAppleToGoogle,
+} from './src/services/storePublishingSync';
+
+async function main() {
+  const snapshot = await fetchStorePublishingSnapshot({});
+
+  console.log('SNAPSHOT_WARNINGS', JSON.stringify(snapshot.warnings || []));
+
+  const apple = snapshot.apple;
+  const google = snapshot.google;
+
+  if (!apple) {
+    console.log('NO_APPLE_SNAPSHOT');
+    return;
+  }
+
+  console.log('APPLE_BUNDLE', apple.bundleId);
+  console.log('APPLE_VERSION_LOCALIZATIONS', apple.versionLocalizations.length);
+
+  for (const loc of apple.versionLocalizations) {
+    const sets = await fetchAppleScreenshotSets(loc.id).catch(() => [] as any[]);
+    const total = sets.reduce((n, s: any) => n + (Array.isArray(s.screenshots) ? s.screenshots.length : 0), 0);
+    const complete = sets.reduce((n, s: any) => n + (Array.isArray(s.screenshots) ? s.screenshots.filter((x: any) => x.assetState === 'COMPLETE').length : 0), 0);
+    console.log(`APPLE_LOC\t${loc.locale}\tversion=${loc.versionString || ''}\tstate=${loc.appStoreState || ''}\tsets=${sets.length}\ttotal=${total}\tcomplete=${complete}`);
+  }
+
+  if (google) {
+    console.log('GOOGLE_PACKAGE', google.packageName);
+    console.log('GOOGLE_LISTINGS', google.listings.length);
+    for (const listing of google.listings) {
+      const sum = google.imagesByLanguage?.[listing.language];
+      const phone = sum?.phoneScreenshots ?? 0;
+      const seven = sum?.sevenInchScreenshots ?? 0;
+      const ten = sum?.tenInchScreenshots ?? 0;
+      const icon = sum?.icon ?? 0;
+      const feature = sum?.featureGraphic ?? 0;
+      console.log(`GOOGLE_LOC\t${listing.language}\tphone=${phone}\t7in=${seven}\t10in=${ten}\ticon=${icon}\tfeature=${feature}`);
+    }
+  }
+
+  const syncResult = await syncAppleToGoogle();
+  console.log('SYNC_RESULT', JSON.stringify({
+    textsSynced: syncResult.textsSynced.length,
+    imagesSynced: syncResult.imagesSynced,
+    errors: syncResult.errors.slice(0, 20),
+    totalErrors: syncResult.errors.length,
+  }, null, 2));
+}
+
+main().catch((e) => {
+  console.error(e?.message || String(e));
+  process.exit(1);
+});
```
